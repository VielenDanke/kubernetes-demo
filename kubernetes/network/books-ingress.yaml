apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: books-ingress
spec:
  rules:
    - host: books.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: books-service
                port:
                  number: 9090